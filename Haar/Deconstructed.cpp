#include "Deconstructed.h"

void Deconstructed::subscripts(){
	for (int j = 1; j < size; j++){
		std::vector<double> temp;
		for (int k = 0; ; k++){
			for (int l = 0; l < k; l++){
				if (l % 2 * k == 0){}
					// what am i doing
			}
		}
	}
}
